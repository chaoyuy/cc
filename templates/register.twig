<!doctype html>
<html lang="en">
{% include  'partials/head.twig' %}
  <body>
  {% include 'partials/navigation.twig' %}
    <div class="container">
      <div class="row">
        <div class="col-md-4 offset-md-4">
        <form id="register-form" method="post" action="register.php">
        <h3> Sign up for account</h3>
        {% if register.erros.email %}
        {% set emailErrorMessage =register.erros.email %}
        {% set emailInvalid = 'is-invalid'  %}
        {% endif %}
        <div class="form-group">
        <label for="email" >Emaill address</label>
        <input id="email" value="{{email}}"type="email" class ="form-control" name="email" placeholder="you@domain.com">
        <div class="invalid-feedback">{{emailErrorMessage}}</div>
        </div>
          {% if register.erros.password %}
        {% set emailErrorMessage = register.erros.password %}
        {% set passwordInvalid = 'is-invalid'  %}
        {% endif %}
        <div class="form-group">
        <label for="password">password</label>
        <input id="password" type="password" class ="form-control {{passwordInvalid}}" name="password" placeholder="minimum 8 characters">
        </div>
        <button type="submit" class="btn btn-primary mt-3">Sign Up</button>
          </form>
    </div>
    </div>
    </div>

{% include  'partials/footer.twig' %}
 </body>
</html>